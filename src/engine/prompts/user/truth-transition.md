Produce one candidate transition covering every action in `state.actionSet.assigned`, using the committed plans and receipts in `state.committedResolutionPlans` and `state.resolutionReceipts`, plus committed checks, random results, events, and the supplied temporal boundary. Emit exactly one outcome for every assigned action, including an action backed by an ongoing or conditional Activity. For fixed, rate and staged Activities, an active Activity must remain continuing until its trusted scheduled completion boundary, unless supported failure or blockage ends it; a completed scheduled boundary must settle with a non-continuing outcome; retain exactly one outcome during repair. Use the assertion evaluation states specified in the system instructions; distinguish operation preconditions from event and outcome witnesses after the transition. Receipt grades constrain settled outcome status and may be mechanic inputs, but they do not certify action completion and are adjudication records rather than in-world causes: never cite a `resolution_receipt` in `causes`; causal kinds are exactly action, check, random, event, fact, law, or mechanic. Use `*Ref` fields with exact existing handles and proposal keys for new entities, facts, events, outcomes, and mechanics. Describe semantic effects with relevant causes and preconditions while leaving deterministic writes and time advancement to the engine, select an active `mechanicContracts` entry by its `mechanicRef` handle and copy its exact input shape when emitting a `mechanicInvocation`, never invent legacy fields or raw numeric deltas, and change only the invocation identified by `task.resolutionScope` during repair while preserving all other valid proposal fields.
Goal and conditional Activities have no scheduled completion time: judge succeeded from the entire original task and supported state/effects, continuing for unfinished valid work, or blocked/failed when it cannot proceed; ongoing Activities remain continuing unless an explicit terminal disposition applies, and neither a checkpoint nor a receipt grade proves completion.
