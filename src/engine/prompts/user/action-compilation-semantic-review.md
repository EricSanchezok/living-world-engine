Review every entry across all seven dimensions: actor-identity, derived-intent, temporal-boundary, quantities-resources, dependencies-audience, knowledge-isolation, open-action-freedom.

The evidence table is a lossless leaf-address representation of the supplied documents. Use only the entry's source/output evidence and shared state/rules evidence. Cite exact evidence IDs, not invented paths or quotations.

Return pass when the relevant source meaning and constraints are preserved; fail for a demonstrated contradiction or unauthorized addition; unresolved when the evidence cannot distinguish a valid from an invalid interpretation. Missing evidence is not automatically failure or success. Desired outcomes and proposed attempts are not committed facts.

Actor-identity: preserve actor and target identity. Derived-intent: preserve the requested attempt, goal and means without inventing completion. Temporal-boundary: preserve onset, duration, stopping conditions and current versus future state. Quantities-resources: preserve amounts, units, direction and resource constraints. Dependencies-audience: preserve read/write targets, participants and intended recipients. Knowledge-isolation: do not give actors unobserved private information; attempting to learn differs from already knowing. Open-action-freedom: preserve creative or compound actions without inventing a closed menu restriction.

Each check needs a brief reason and evidenceIds including at least one source and one output atom, plus any state/rule atoms used. IDs establish provenance, not correctness: explain the relation. For unresolved, identify what evidence is missing and cite the available source/output. Do not judge omitted irrelevant engine boilerplate; if a necessary fact is unavailable, report unresolved.

Return exactly one row per entry id and one check per dimension. Do not include overall verdicts, expected labels, scores or extra entries.
