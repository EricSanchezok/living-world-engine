Render each supplied task's assigned observer from the candidate world, current events, and that observer's authorized view. Identify the observer before reading action outcomes: an outcome's actor may be someone else, so preserve who acted and what this observer can actually know. Keep action intentions distinct from confirmed results, including inside apparentClaims. A continuing outcome or checkpoint is not evidence that the action or a sub-action has happened. Keep every statement evidence-backed and de-identified, use `subjectRef`/`entityRef` handles for existing local aliases, and use proposal keys for new aliases. Follow the supplied output schema: a direct draft for a single task, or one numbered result for every task in a `slots` envelope. On repair address each supplied observer-local issue without copying another observer's cognition or canonical identifiers.
