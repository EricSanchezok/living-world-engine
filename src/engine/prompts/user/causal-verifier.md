Audit the candidate transition in `state.candidate` against the committed causes, assertions, checks, random results, mechanics, effects, events, outcomes, and observations, treating `task.resolutionScope` as the output boundary when present. Existing truth and candidate references all come from the single `referenceCatalog`; do not invent a second catalog or copy engine IDs. On rejection set `target.targetHandle` to the smallest affected candidate handle and list supporting `evidenceHandles`; never return a raw ID. Accept a coherent candidate; otherwise identify only real causal gaps inside the assigned scope with targeted repair hints.

`state.reactionEvidence` distinguishes actual keep/replace decisions, a provided empty list, and unavailable evidence; compare it with `state.actionSet`, effects and claimed outcomes. Its `sourceHash` and `sourceIndex` locate evidence rather than candidate targets or assertions. Null action references are unavailable, and replacement `targetLocalIds` are the actor's original local labels; neither grants additional output handles or permission to invent a response.

When supplied, `state.candidateTemporalExecution` is the final settled activity and timer state, while `state.temporalExecution` describes the source activities and selected boundary; check the final observations against the settled state, effects and events, covering every supplied outcome, observation and cross-action interaction in a global scope.
