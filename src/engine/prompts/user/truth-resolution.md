Commit exactly one grounded plan for each assigned action, request only the declared random results still needed, and finish without changing a committed plan; when `state.committedResolutionPlans` is empty the only legal directive is `commit_plans`, and when it is non-empty the only legal directives are `request_random` or `done`. When `task.resolutionScope` is present it limits output responsibility rather than context visibility; committed plans and receipts are in `state`, never in `task`; use action/entity/mechanic handles from `referenceCatalog` for every existing reference. Each field must use the handle kind named by that field (`targetRefs` are entity handles, source `{ kind, ref }` pairs must match, and effect profile refs are mechanic handles); each `means[].source` may cite only the assigned action, a law, or a ref in the matching `state.dependencySet.assigned` record, and an automatic no-effect plan may use an empty `means` array. A random request must use a fresh `proposalKey`, select an authored `random_distribution` with `distributionRef`, and express causes as `{ kind, ref }` handles for already available evidence; never copy runtime ids or `distributionId` into a model response. New plans, checks, mechanics, and effects use `proposalKey`; never copy runtime ids into a model response.
Resolution mode is a strict contract: `automatic` and `blocked` require both `difficulty: null` and `actorRatingRef: null`; `check` requires a non-null `difficulty` and permits only the actor's listed rating in `actorRatingRef`. A plan cause may be only an existing action, event, fact, or law; checks, random results and mechanic invocations are post-plan evidence and cannot be plan causes; entities, ratings, conditions, and placements may ground means or factors but are not causal-reference kinds; factor roles are equally strict: `permission`, `secondary`, and `risk` require `direction: neutral` with `steps: 0`, `control` requires a helpful or hindering direction with exactly one step, and `potency` or `protection` requires a helpful or hindering direction, a non-null matching effect channel, and one step for semantic evidence, while only authored evidence may carry two steps.
`state.actionSet.available` and `state.dependencySet.available` contain the complete semantic set, while `state.actionSet.assigned` and `state.dependencySet.assigned` identify the assigned output slots, so a repair scope must return exactly one plan for each listed `selectedActionRefs` without regenerating committed plans;
When the repair target is a plan, `state.candidateResolutionPlans` contains the prior accepted candidates; return exactly one replacement plan for that target action while preserving unrelated plans and committed evidence, and do not broaden the repair scope.
