Return a flat transition batch with exactly six root arrays: slots, outcomes, mechanicInvocations, operations, events, decisionRequests. All six arrays are mandatory even when empty. slots lists each original slot number exactly once; do not emit nested slot/result objects. Every non-outcome row retains every canonical field and adds its owning slot number. No row for a slot in an explicit category means that slot's corresponding category is empty.

The JSON Schema defines the complete causalAssertion type once in definitions and uses local $ref entries wherever that same type is required. Each reference has exactly the same meaning as the expanded definition. Emit the actual typed assertion object there, never a $ref string or schema object. All assertion variants and requirements remain available.

Use task.transitionWorklist as the complete coverage index. Its action records repeat the original state.actionSet.assigned for each slot without changing their meaning or authority. Return exactly one outcome per actionIndex. An outcome uses actionIndex instead of slot and actionRef; the decoder restores those two identities from that exact source action. Keep proposalKey, status, summary and causes explicit. Every outcome requires firstAssertion (one complete typed assertion object) and additionalAssertions (an explicit array of every remaining assertion, empty only when there are no more). These restore the canonical nonempty assertions list in exactly that order. Do not emit assertions inside an outcome or omit its firstAssertion; other categories retain their original assertions arrays. Select supported, relevant predicates and exact values yourself: the codec supplies no evidence. A clock or unrelated office fact alone does not establish goal completion. Cause references retain their original canonical shape and handles; an actionIndex is not a cause handle. Do not omit actions, duplicate indices or supply slot/actionRef inside an outcome.

Every assigned slot retains its complete original context and reference scope. Other categories explicitly select their original owning slot; shared state does not authorize foreign references or effects. Preserve compound goals, prerequisites and audiences. Propose supported persistent changes in operations or mechanicInvocations: assertions and outcome summaries do not apply writes. An unfinished goal remains continuing; the checkpoint alone cannot establish success. The codec supplies no effects, status decisions or semantic judgments. Return JSON only, without reasoning or an empty example substituted for the required outcomes.
