In this global planning request, allowedMeansSources is a lossless shared inventory reference. Read meansSourcePool.inventories[allowedMeansSources.inventory]; each row is [recordIndex, sourceSelector]. Its complete original source record is meansSourcePool.records[recordIndex], including any exact factEvidence. Add that row's sourceSelector. When sourcePositions is ordinal, the row's zero-based position is its sourcePosition; when absent, no sourcePosition field is implied. Use only THIS action's inventory and the original output schema. Inventory/record indices are input lookup coordinates, never output source positions or evidence handles. Repeated inventory references denote exactly the same ordered list. Every original source, fact, choice, position and action remains available; sharing supplies no semantic evidence or permission.
