In this global planning request, allowedMeansSources is a lossless shared inventory reference. Read meansSourcePool.inventories[allowedMeansSources.inventory], which contains actionRef and an ordered recordIndices list. At each position, its complete original source record is meansSourcePool.records[recordIndices[position]], including any exact factEvidence. When sourcePositions is ordinal, that zero-based position is the sourcePosition required by the original output schema; when absent, no sourcePosition field is implied. Use only THIS action's inventory. Record indices are input lookup coordinates, never output source positions or evidence handles. The engine reconstructs each original sourceSelector with its existing action-owned selector function: m: plus the first twelve hexadecimal characters of SHA-256 of canonical JSON {actionRef,source:{kind,ref}}. These hashes are not requested by the indexed output schema; do not compute or output them. Repeated inventory references denote exactly the same ordered list. Every original source, fact, choice, position and action remains available; sharing supplies no semantic evidence or permission.
