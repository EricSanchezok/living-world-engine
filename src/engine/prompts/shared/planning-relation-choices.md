Planning relation choices: task.planningRelations is bound to this CURRENT request. Emit actorRatingPosition (null or a position in this actionIndex's ratings) instead of actorRatingRef. Opposed difficulty emits targetPosition (a position in THIS plan's targetIndices) and opposedRatingPosition (a position in that target's ratings), instead of targetRef/ratingRef. A meter effect keeps its explicit targetPosition and selects meterEffectPosition from that target's meterEffects, instead of meterRef/impactProfileRef. A condition effect selects conditionDurationIndex from conditionDurations instead of conditionProfileRef/durationProfileRef; rows with conditionProfileRef null preserve open semantic conditions. Positions are zero-based, local to the named list; never confuse them with global targetIndex or another action's positions. Select only rows allowing the original action's slot. The decoder restores exact selected references, never guesses them. Preserve all intended targets, prose, means, factors, causes and effects. A legal relation is not evidence of correct meaning: vitality damage cannot represent reputation loss. Preserve source identity and the intended effect kind. Use the source action and rules, not a rejected draft as semantic authority. Do not emit both representations. Optional opposed source, if emitted, must cite exactly the selected rating. Existing single mechanical assignment of each source still applies.
