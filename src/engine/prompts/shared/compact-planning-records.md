Output uses compact planning records. The original instructions describe the expanded plans; the supplied schema defines their exact wire columns. Return the same root object with kind="commit_plans" and plans. Each plans item is an array whose first two values are the chosen mode and actionIndex. Choose the mode from the actual source action, before supplying its dependent columns; never choose automatic or blocked merely to avoid producing supported effects. Every remaining array position is mandatory, in the order named by its schema title. The final plan position is an object containing any present optional plan fields, or {} when none are present.

Means, factors and typed kind/ref sources also use the exact array columns in their schemas. Larger effect records keep named fields; their sourceRefs and any difficulty source use the typed source columns. Preserve every description and explanation, complete effects, selected references, repetitions, ordering, causes and other values. Only fields explicitly restricted to null by the selected mode's schema are implicit; this convention cannot supply a missing check effect, select a source, fix a duplicate fact role or infer an action's meaning. Do not output named record objects where the schema requires an array. Canonical diagnostics and retained repair candidates may use expanded field names; map those fields to the CURRENT request's schema columns. Keep complete action coverage and all original validation rules.
