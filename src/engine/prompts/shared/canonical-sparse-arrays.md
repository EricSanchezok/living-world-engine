The root outcomes array and every outcome field remain required. At this wire boundary only, mechanicInvocations, operations, events and decisionRequests may be omitted when they contain no proposed items: omission explicitly means []. An explicit empty array has the same meaning. Emit every supported nonempty list in full; do not use null, omit an intended consequence, or replace a needed write with an outcome summary. This convention supplies no action, fact, assertion, reference, event, operation, success status or semantic judgment. All nonempty items, nested required fields, source permissions and causal checks retain their original meaning.
