Generate the complete initial plans and one provisional transition in the SAME response. This is uncommitted co-generation, not permission to skip any planning duty or review. The response has kind, plans, and provisionalTransition. Every plan retains additionalRandomness. Return provisionalTransition:null whenever any plan needs a check, declares additionalRandomness:"defer", or the transition depends on evidence not yet available. Do not downgrade difficulty, omit an effect or suppress randomness to use fusion.

state.stageContexts is a lossless two-context encoding. Reconstruct stage 0 (planning) and stage 1 (pre-commit transition) by recursively overlaying each slot delta on shared; absent object keys inherit, arrays and scalar values replace. Restore each referenceCatalog.candidates dictionary in its catalogCandidateOrder (or the order selected by catalogOrderRef in catalogOrders). These two contexts describe successive tasks for the SAME assigned actions; they are not independent action groups. The outer actionSet and task.resolutionScope retain the assigned component. Any outer physical slots remain independent.

Apply the original planning instructions to stage 0 and the original transition instructions to stage 1. The latter intentionally contains no committed plans or receipts yet. For a provisional transition, use the plans you emit in this response as proposed inputs, with no invented roll or random result. Existing references come only from the supplied catalogs; do not fabricate handles for future plans, receipts, mechanics, facts or events. New transition objects use the ordinary proposal reference contract. The engine alone derives and settles receipts, applies typed mechanics and advances time after independent plan review.

A no-effect automatic receipt does not prove that an attempted task happened. Preserve every required action and all meaningful realized speech, investigation, movement or other world consequences in the ordinary operations and events when supported. A request for information requires supported information delivery or a grounded obstacle; a success label and elapsed time alone do not establish that result. Do not invent knowledge, consent or an answer from the actor's wish. Legitimate waiting, failed attempts and already-satisfied goals retain their actual semantics.

The combined output schema owns the response envelope. Do not return two separate JSON objects. All canonical schemas, references, causes, assertion timing, knowledge limits and temporal completion rules from both tasks still apply.
