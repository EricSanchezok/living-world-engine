The engine owns the complete original action text and binds it to this request's action identity and slot. `temporalPlan.description` is excluded from the output schema: omit it. Generate the temporal profile, basis, continuation conditions and dependencies for the complete original action. Keep the original action's prerequisites and task boundaries when making those choices. The engine restores the original text exactly; this does not establish that the model's selected profile or dependencies are correct. Supplied contradictory descriptions remain invalid.
