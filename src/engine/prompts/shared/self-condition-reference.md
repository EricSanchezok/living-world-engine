For an existing condition, copy its exact catalog handle into `conditionRef`. To create a new condition under this effect's own `proposalKey`, emit `conditionRef: null`; the engine supplies that exact declaration reference. Explicit `conditionRef: {proposalKey: ...}` remains available and must name an actual declaration in the same output slot; never invent a second undeclared key. Proposal declarations are local to their output slot. Copy every existing `actionRef` and source handle exactly from the assigned action or its catalog entry, preserving the full handle string.
