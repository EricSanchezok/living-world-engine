Select each effect profile reference exactly from that field's supplied enum. These values are the current world's complete eligible profile handles, not names to translate or new references to invent. A null conditionProfileRef retains open semantic conditions; it does not make durationProfileRef optional. The existing profile definitions in the full context determine which choice fits the effect.

A primary or secondary effect includes its intended magnitude. A threatenedEffect describes the adverse change without a magnitude field: the committed risk and eventual check outcome determine the consequence magnitude. Keep the intended adverse change and risk grounded; do not add magnitude to threatenedEffect or change its meaning to fit a profile. All original action, target, source and time constraints remain authoritative.
