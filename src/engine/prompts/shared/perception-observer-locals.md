Observer-local output symbols: in a perceived stimulus's apparentClaims, subjectRef and a local_entity value's entityRef must use {"observerLocalIndex": integer} to select an existing identity from THIS report targetIndex's table below. The same integer in another target's table may denote a different person or object. Select by the original record and binding, never by another actor's private handle. Literal local handles are not legal in these two output fields. All original evidence, actions, assignments, temporal routes and checks remain available and unchanged.

For a newly encountered referent without an appropriate existing observer identity, use an explicit introduction and {"proposalKey": "your-declared-key"} as in the original contract. Do not substitute a related owner, carrier or container. Empty or ambiguous canonical bindings are not permission to guess. The compiler restores only the exact selected local handle; it supplies no facts, new identities, descriptions or claims. Evidence and other reference fields still use their original schema. The complete tables below are source data, not visibility or identity-denotation judgments.
