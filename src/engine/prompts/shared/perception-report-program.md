For this request, compile the original perception task into the supplied perception_program schema. This schema replaces only the response syntax: the original context, assigned pairs and perception duties remain authoritative. Do not return a check-or-done directive directly.

Declare only the initial perception checks that the complete source justifies. An assigned pair is not a required check, and a successful check cannot create a missing sensory route or reveal a future action's completed result. Each request keeps the original check schema, exact observer-owned rating and a relevant existing Fact or Law cause in addition to its source Action. Its proposalKey is the program-local checkKey. Numeric calculation and every actual die result remain engine-owned.

Give exactly one root for each original targetIndex. Each root names an index in nodes. A report node contains the original complete report with checkKeys in place of future committed checkRefs. A branch node names one declared checkKey and the node indices for its succeeded and failed outcomes. The engine follows that branch only after the real committed result exists. Report and branch check dependencies must belong to that same observer and source action. A perceived report citing a check must lie on its succeeded branch; do not emit unconditional perception for declared uncertainty. Negative reports may cite the relevant checked uncertainty or use their original independently supported evidence.

Write only source-supported conditional reports. A conditional stimulus is a hypothesis for that actual branch, not an observation that has already occurred. Preserve observer-specific identity, information access, evidence, introductions and claims. No future event, person, delivery, successful investigation or intended task result becomes current perception merely because it appears in the attempted action or a proposed check. Keep source-grounded reports concise without dropping required content.

If a justified decision needs further interpretation or additional checks after these results arrive, use a defer node with its same-pair checkKeys and a concise reason. Deferral preserves the ordinary full-context model continuation for the whole report decision; do not fabricate a terminal answer just to avoid it. A program with no checks must finish through report nodes and cannot defer. Branches may share existing nodes, but every node and check must be used, all references must resolve, and no cycle or repeated test of a check along one path is allowed. Do not embed expected dice, select a random outcome, or revise checks after observing results. Return only the program object.
