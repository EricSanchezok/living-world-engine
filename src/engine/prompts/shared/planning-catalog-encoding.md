The physical state's catalog uses shared-json-v3-catalog-v1 when template encoded, otherwise shared-json-v3-prefix-v1. Reconstruct each complete logical slot before applying its original task, evidence, permissions and validation rules.

When state.repairDiagnosticDomains is present, first restore the complete historical cause domains. At each registered locations[].path in state, rejectedDomain: {sharedRejectedDomain: n} means the full ordered array state.repairDiagnosticDomains.domains[n].values. The table stores every original row, including its old index position, label and source slots; its hash and sourceHash bind the evidence. Restore those occurrences before expanding catalog records and logical slots. Do not interpret similarly shaped unregistered values as references. Historical domains explain rejected choices only: they never grant current permissions. Make repair selections from the CURRENT task.planCauseChoices domain and current component scope. No error, prior output, originalValue or old-domain row has been removed. Return the unchanged output schema, without encoding markers or the diagnostic table.

For shared-json-v3-catalog-v1, first expand state.shared.referenceCatalog.candidates. Its rows map each original candidate key to [templateIndex, originalFields]. Merge templates[templateIndex] with originalFields to recover the shared record. originalFields contains only the handle, label and statePath fields that were present; never add an absent field. Every other original field, including kind, meaning, allowedUses and visibility when present, remains in its template. Apply each slot.delta to that expanded shared context exactly as in shared-json-v3.

Each slot's complete candidate order is catalogOrderPrefix followed by catalogOrderSuffixes[slot.catalogOrderRef]. Neither the prefix, suffix, shared table nor a template alone defines a slot's permission domain. Use the reconstructed catalog for that slot only. All source hashes, candidates, ordering, facts, actions, rules, assignments and repair information are preserved. These instructions change the input representation only; return the unchanged required output schema and preserve all source semantics.
