The physical task's complete responsibility is task.transitionWorklist.actions: produce one outcome for EVERY listed actionIndex. Each row contains the exact original action, its matching committed plans and adjudication receipts, matching activities and the trusted temporal boundary. participantEntities repeats the canonical actors and targets already named by those plans, with their exact current placements; placementAncestors gives their original visible location ancestry. inputFacts repeats every original fact about those participants as well as facts explicitly cited by the plans, receipts and activities. This is neither a complete world view nor proof that other constraints do not matter. All original context, evidence and allowed references remain in the corresponding reconstructed slot.

physicalEnvelopeAudit holds empty batching placeholders for transport verification. It is NOT an empty logical assignment or an empty permission domain. The real assignments and catalogs are those in the original slot contexts, and the complete action coverage is the worklist. Never derive zero required outcomes from physicalEnvelopeAudit. Preserve each slot's reference permissions and read other relevant state and rules as needed.

state uses shared-json-v3-catalog-v1 when its shared candidate map is template encoded, otherwise shared-json-v3-prefix-v1: the complete ordered catalog for each slot is catalogOrderPrefix followed by catalogOrderSuffixes[slot.catalogOrderRef]. Neither part alone is the slot's permission domain. Combine shared and slot.delta exactly as in shared-json-v3; all candidates, ordering, fields and source hashes are unchanged. The prefix is stored once solely to avoid repeating identical reference strings.

Receipt grades are adjudication, not completed work. Apply the provisional receipt and assertion-state contracts. A goal checkpoint does not complete a task; an unfinished valid action still requires a continuing outcome. Persistent changes require actual supported operations or mechanics. Keep every original quantity, prerequisite, audience and compound goal, without inferring effects or completion from this evidence view. Indexed identities remain unchanged. Keep the canonical outcome summary and every other required output field explicit.

Permission or authority to attempt work is not evidence that its physical, timing or audience prerequisites are satisfied. Check the original ordered/conditional action against current participant state before describing a subtask as underway. Active Activity status and automatic receipt grades do not establish contact, completed preparation, arrival or a satisfied condition. If a required prerequisite is still pending, preserve it explicitly as waiting/preparation rather than narrating the gated subtask as already happening. Describe actual effects only when the source state or proposed operations/mechanics/events support them. Do not invent writes every tick, additional knowledge, communication or completed substeps merely to show progress. Canonical facts remain Truth evidence; they do not become an Agent's observed knowledge by appearing here.

For shared-json-v3-catalog-v1, expand state.shared.referenceCatalog.candidates before combining the shared context with each slot.delta. The candidates table has templates and rows. For each original candidate key, rows[key] is [templateIndex, originalFields]; merge templates[templateIndex] with originalFields to recover that shared record. originalFields contains only the handle, label and statePath fields that were present; do not add an absent field. Every other field is in the template, including exact allowedUses, meaning, kind and visibility when present. Apply the original slot delta after expansion, then use the complete ordered catalog from the prefix and slot suffix. Neither a template alone nor the shared table alone determines a slot's permissions. No reference, candidate, rank, state fact or rule has been removed or renamed.
