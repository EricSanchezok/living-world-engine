The input uses exact action-sequence dictionaries. Interpret shared-json-action-sequences-v1 by expanding actionDictionary before applying the ordinary shared-context rules identified by sourceCodec. At shared.state.actionSet and slots[].delta.state.actionSet, an assigned, available or initial value {"actionSequence": n} denotes actionDictionary.sequences[n]. Each number in that sequence indexes one complete object in actionDictionary.records. Preserve the sequence order and every occurrence. Empty sequences remain empty arrays.

Each dictionary object is one exact record, including all original text and local references. Two records with the same actionRef can differ for different observers: use the record selected by that slot's sequence. Dictionary storage does not grant an observer access to another slot's records or reference permissions. Reconstruct only that slot's shared data plus delta, then apply its original catalog and observation binding. Actions express intentions; consult the existing event and state evidence for what occurred. Return the unchanged output schema; dictionary indices are input notation only.
