Plan source selection representation: In each plan.targetRefs, copy targetSelector values from target-eligible entity entries in this slot's referenceCatalog. In each means.source, copy a sourceSelector string from the matching plan.actionRef record in state.actionSet.assigned.allowedMeansSources. Selectors identify exact existing evidence; inspect its kind/ref and meaning before choosing. Do not copy a source selector from another action. All original choices and action requirements remain in force. Return every required plan and preserve the complete action meaning. Other output fields, including actionRef, factors and causes, retain their stated canonical representation; this representation changes only targetRefs and means.source. Repair diagnostics validate the decoded canonical result; an unresolved selection is invalid and requires an explicit valid selection from the same inventory, never omission of intended meaning.
