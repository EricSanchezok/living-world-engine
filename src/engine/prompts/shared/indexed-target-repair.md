The following indexedTargetRepair diagnostics map rejected canonical fields to this request's generated fields. A canonical effect.targetRef error is not a request to emit targetRef: this wire requires plan.targetIndices and effect.targetPosition. Select every intended entity explicitly from the CURRENT task.planningWorklist.targetChoices, within the action's source slot, and place its targetIndex in that plan's targetIndices. Each non-null effect's targetPosition is a zero-based position in that same selected list. An empty list has no position zero. Previous positions and decoded values describe a rejected request; never reuse them as current target indices. Preserve the original action, intended subjects, effects, prose and all other constraints. No diagnostic chooses an entity or proves that an effect is semantically justified. Keep every original issue and correct every affected plan; do not discard effects or invent convenient targets to obtain acceptance.
