For this compilation response, readCandidateKeys is the original requiredExistingCandidateKeys field and writeCandidateKeys is the original potentiallyAffectedCandidateKeys field. Use the new field names shown by the response schema. All original candidate permissions, slot ownership, action meaning and temporal requirements still apply.

Choose reads and writes independently. A read names existing state needed to interpret the action. A write conservatively names an existing record whose state or protected scope this action could affect or conflict with; it does not perform that change. A key may be in both sets. Being mentioned, being the location of an action, or containing someone the actor addresses is not by itself a write to that record. Preserve source-supported possible effects, shared-resource contention and broad scopes when actually warranted; do not omit a possible conflict merely to make a smaller component. Do not infer that every read must also be a write.

The engine adds mandatory actor/location dependencies and validates later actual effects against the declared scopes. You still must identify the action-specific dependencies. Future records cannot be named by invented existing candidate keys. Do not create effects, certify concurrency or change the action to fit a smaller footprint. Return the complete original compilation result with only the two dependency field names replaced.
