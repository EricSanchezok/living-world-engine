`candidateKey` is an exact request-local selector. Its prefix states the catalog record type: for example `agent_r010`, `entity_r335`, or `action_r154`. Copy the complete key from this request. An Agent is a decision-making participant; an Entity is a world object or embodied character; an Action is an issued intention. These record types are distinct even when their labels describe the same character. Do not exchange their prefixes or infer one key from another. `audienceAgentCandidateKeys` selects Agent records. State dependencies select the allowed canonical state records, not Agent or Action records. An alias retains its exact meaning; all catalog field-use and slot permissions still apply. The engine resolves the selected key after validation.
