Output slots as an object keyed by the exact request slot number, not an array; omit the inner slot field. Choose reference keys only from the field's schema enum and the corresponding slot's visible catalog. Conditional temporal profiles require at least one continuation assertion. These are representation constraints only; do not change the action's meaning, actor, targets, quantities, or freedom of action.
