The output schema's source_bound_plan_action definition enumerates only the actions assigned to this physical request. Produce exactly one plan for each assigned action and none for merely available actions elsewhere in the context. The source_bound_plan_target definition lists existing target selector choices, not spelling examples; copy an exact choice for each intended target instead of constructing a selector from a name or another kind of reference. These root inventories do not expand any original slot's permissions. Preserve the full action meaning and use the original slot context and action-owned means sources. Existing ownership, coverage, factor and effect validation still applies.
