Use plan.targetRefs to select exact canonical entity handles from the complete task.planningWorklist.targetChoices rows. Inspect each handle, label and allowed slots against the original action and world; preserve target order and repetitions. Select only entities allowed in that action's original source slot. For each non-null primaryEffect, secondaryEffect or threatenedEffect, emit targetRef as the exact entity handle present in this plan's targetRefs. Choose the subject supported by the action and state; an available meter or profile never authorizes moving an effect to another subject. Emit no plan.targetIndices or effect.targetPosition. Existing targetIndex annotations describe the source inventory only and are not output choices. Action indices, action-local means positions and cause indices retain their separate supplied contracts. Empty targetRefs is valid only when no effect needs a target. Canonical references change spelling only, without granting new facts, knowledge, effects or temporal completion.
