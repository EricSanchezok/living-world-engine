The engine owns `temporalPlan.description` and binds it to the complete original action text for that slot. Omit this field from your output. If you supply it, it must match the original text exactly; do not paraphrase, summarize, add subtasks or reinterpret prerequisites as new orders. Select the temporal profile and dependencies for the complete original action. Source-owned description does not establish that the selected profile or dependencies are correct.
