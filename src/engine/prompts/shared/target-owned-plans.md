Emit plan.targets as an ordered array of {entityRef, effects}. Select entityRef by copying the exact existing handle from task.planningWorklist.targetChoices, within this actionIndex's original source slot. Use the readable entity handle, not targetIndex, sourcePosition or a position in another list. Preserve every explicitly intended target, including targets with effects:[] and repeated targets.

Each effects entry is {role, effect}, where role is primary, secondary or threatened. Place the complete effect body directly under its intended subject entityRef. Containment supplies the effect subject: do not emit targetPosition or targetRef inside effect. Each role occurs at most once across this plan's targets; do not copy one role onto multiple subjects. The primary, secondary and threatened roles correspond exactly to the canonical primaryEffect, secondaryEffect and threatenedEffect requirements. Absence means null; all original mode, magnitude, consequence, evidence and temporal constraints still apply. A required effect cannot be omitted. Retain every other effect field and all supporting evidence. Do not emit plan.targetIndices, targetRefs, primaryEffect, secondaryEffect or threatenedEffect alongside targets. Do not choose an entity merely because it appears at the same number in another choice domain.
