Return a complete transition object with five sibling arrays: `outcomes`, `mechanicInvocations`, `operations`, `events`, and `decisionRequests`. Include every field, using an empty array only when that category has no entries. In a slot batch, all five arrays belong inside each slot's `result` object; close that complete object before starting the next slot. Preserve the required outcome for every assigned action. Represent supported persistent changes through the allowed operations or mechanic invocations; an outcome summary alone does not apply those changes. Check the supplied completion boundary before choosing each outcome's status.
