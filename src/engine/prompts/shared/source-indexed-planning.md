Physical planning wire: return {"kind":"commit_plans","plans":[...]} with exactly one plan for every actionIndex in the outer task.planningWorklist.actions. Do not emit slot/result wrappers or plans for merely available background actions. Each row retains the complete action, its original slot and its action-owned means sources. Use actionIndex instead of plan.actionRef; the decoder restores that exact action handle and original slot. Output order does not change ownership. Duplicate, missing, unknown or cross-request action indices fail.

Use plan.targetIndices to select targetIndex values from the complete task.planningWorklist.targetChoices rows. Inspect the original handle, label and allowed slots; retain every intended target, and do not use a choice outside the action's source slot. For each non-null primaryEffect, secondaryEffect or threatenedEffect, use targetPosition instead of targetRef: this is the zero-based POSITION IN THAT PLAN'S targetIndices, not a global targetIndex. The decoder binds that effect to the exact entity you selected at that position. Choose subjects and effects from the original action and state evidence; do not assign an effect to a convenient different subject merely to make the output valid. Empty targetIndices is valid only when no effect needs a target.

Each means.source remains the exact sourceSelector from this action's allowedMeansSources. Never borrow another action's means selector. All other fields, including factors, opposed difficulty, causes, ratings and condition references, retain their supplied schema and source-evidence requirements. Original action text, temporal boundaries, future checkpoints, cognitive isolation and source-slot authority remain binding. Indices change reference spelling only; they do not authorize skipping, merging, retargeting or adding actions, inventing outcomes, or treating a future goal as already achieved. The complete original context remains available. task.planningIndices and worklist hashes are provenance, not world facts. Repair candidates and paths retain their original reference coordinates: resolve their action handles against the CURRENT worklist and use CURRENT indices, never indices from an earlier request. Exact required cardinality, original responsibilities and canonical validation still apply.
