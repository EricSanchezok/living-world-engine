Read the outer physical envelope's task.planningWorklist as the explicit work queue for this request; this index belongs to the outer envelope, not to a reconstructed slot. Its actions array copies every assigned action record in full and states its original slot. Produce exactly actionCount plans: one for each listed action.actionRef, preserving that action's full meaning and source-slot responsibilities. Available actions elsewhere in the unchanged background are evidence, not additional work. The complete targetChoices index maps exact targetSelector strings to their original entity handles and labels; its slots list is the set of source slots allowed to use each choice. Copy a permitted selector when choosing a target; do not manufacture an identifier from the label. Use each action's own allowedMeansSources and its complete reconstructed source context for constraints, state facts and effects. sourceSlots and sourceContextHash bind this projection to that preserved source; they are provenance, not world facts. The worklist does not authorize skipping, paraphrasing, merging, retargeting or adding actions, and it changes none of the existing output or semantic validation rules.
