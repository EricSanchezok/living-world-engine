For opposed difficulty, select the actual opposing entity's existing `ratingRef` from `task.opposedRatingChoices.rows`, checking its displayed owner and this action's source slot. Emit `targetRef: null` to explicitly select that rating's displayed owner; the decoder supplies that exact entity reference. This is the opponent's aptitude, not the acting character's aptitude. The owner must still appear in the plan's explicitly selected targetIndices; never add an unrelated owner merely to fit a rating. An explicit non-null targetRef retains its exact meaning and must match the selected rating's owner. Do not guess absent ratings or change an invalid explicit pairing. Prefer omitting difficulty.source; it is restored exactly as {kind: "rating", ref: ratingRef}. Environment difficulty and its independently selected source remain available, including when an intended opponent has no eligible rating. Actor rating selection remains separate and must belong to the acting character. Optional copies of baseEffect or opposed source retain their original exact-consistency rules. All original action, interval, effect, factor, source and slot constraints remain binding.
