Return one flat planning object: {"kind":"commit_plans","plans":[...]} containing exactly one plan for each assigned actionRef across all input slots. Do not emit slot or result wrappers. The decoder uses each plan's explicit actionRef to restore its unique original slot; you do not choose or change that ownership. Preserve every plan field and keep each action's meaning, references and responsibilities within its original reconstructed slot context. Interleaving plans from different slots grants no shared causal or semantic authority. Unknown, cross-root, missing or duplicate action references reject the response. Prior candidates and error paths can use canonical slot/result wrappers for traceability; those are feedback evidence, not the requested output shape. Apply the supplied flat output schema while resolving that evidence against the original slot and action.
