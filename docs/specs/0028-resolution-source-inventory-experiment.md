# Resolution Source Inventory Experiment

Artifact-Version: 1
Status: Approved

The user's renewed instruction to continue optimization authorizes this bounded STEP-E1-C1 candidate within the existing independent CNY 1000 balance. [The continuation record](0027-single-magnitude-resolution-experiment.md) owns authorization and [STEP-E1](0026-full-step-efficiency-experiment.md) owns gameplay and budget gates. This is an independent hypothesis, not promotion of the failed magnitude or feedback treatments.

## Intent

Make the existing means-source boundary explicit beside each assigned action before the first model response. Avoid paying for repair merely to reveal a mechanically computable source inventory.

## Contract

The opt-in `source-inventory-truth-resolution` candidate adds `allowedMeansSources` to assigned resolution action records. Each entry copies an existing catalog handle and its type; entries include the action itself, laws, and existing entity, fact, condition, rating or placement sources covered by its committed reads or writes. Explicit global fallback uses its existing broad non-action source scope. Audience membership alone does not add a source. This expresses the existing prompt boundary; it is not a claim that every other action reference is rejected by every kernel predicate.

The model still chooses whether and how a listed source supports the means. The list selects no evidence, adds no world fact, replaces no output and grants no reference permission. All canonical state, actions, catalogs, dependencies, instructions, output schemas, validators, batch sizes, repair limits and other model settings remain intact. There is no new model call or parser recovery. Initial, available and non-resolution projections remain unchanged; repair uses the corresponding assigned action and its unchanged grounding. Missing grounding fails preparation rather than supplying an empty or guessed list.

## Plan

Validate deterministic source inventories and exact removal of the additive field back to the reference context. Exercise registered execution and batch/repair scope before committing. Freeze a new paired model probe with immutable inputs, settings, scoring and budget before sending requests. A full-world diagnostic requires its prospective gate; only real committed and replayed trajectories satisfy gameplay acceptance.

The full-world runner binds an explicitly supplied continuation identity, authorization and deadline separately from the unchanged historical protocol and budget hashes. An optional inventory selection changes only the resolution child; direct context, compilation, batching and recovery retain their pinned choices. A combined inventory and inference experiment receives its own prospective identity and gate; its result does not establish either ingredient's isolated effect. No stopped candidate is automatically resampled or promoted.

The combined inventory/low runner verifies the frozen preflight file hash, completed paired sequence, model-mode evidence and every prespecified mechanical gate before preparing a trajectory. A separately persisted full-world variant binds low thinking only to the resolution profile in addition to the existing grounding and transition selections. The verifier and other profiles retain their inference settings. Restoring only those declared profile bindings must recover the complete original world template; historical catalogs and instances remain immutable.

The shared-context runner requires independently frozen inventory-batching and observation evidence before selecting a complete-world candidate. It checks each report against its preflight file hash, request sequence, six-source coverage, inference evidence and prespecified pass/token gates. The observation candidate requires the separately frozen perspective probe and source-bound inspection of every candidate response. A finite mechanical pass cannot override a determined attribution or perceptibility violation. The review binds the exact source context and output hashes, and the runner rejects a role prompt that differs from the admitted preparation. If low observation thinking is selected, a separately persisted catalog binds it only to the observation role; no other role inherits it. Both shared batching children retain the registered lossless context codec, while each logical observer's knowledge and references remain isolated. The observation prompt follows the physical schema without changing its one-observer logical responsibility. A fresh continuation and candidate identity bind the combined diagnostic; neither development gate establishes whole-game success.

An observation-only physical slot table may copy existing observer/local-entity handles and exact actor-to-action identity relationships beside the task. Self aliases come only from the supplied local entities' canonical bindings. This additive layout does not replace the complete encoded contexts, change other roles, infer perceptual access, assign a new reference or repair model output. The frozen probe and source-bound review cover its non-self observer case before whole-game admission.

## Verification

Cover own versus other actions, read and write dependencies, audience-only exclusion, matching reference types and uses, global fallback, missing grounding, repair subset identity, full-context preservation and unchanged physical batching. Run relevant tests and `npm run check:fast` before a local commit. The ledger retains every attempt, and the Notion record retains failed candidate results and inference limits.

## Evidence

STEP-E1 evidence binds trajectory 11 and the closed 24-request magnitude comparison. [The source inventory](../../src/engine/contracts/resolution-source-inventory.ts) is a deterministic projection of existing prompt data; the original materializer remains authoritative.
