# Source Role Gameplay Candidate

Artifact-Version: 1
Status: Approved

## Intent

Under the delegated [non-thinking gameplay experiment](0029-nonthinking-gameplay-efficiency-experiment.md), evaluate the source-role clarification from the [frozen diagnostic](0064-resolution-source-role-diagnostic.md) in a fresh complete-world run. The diagnostic supplies limited mechanical evidence, not an open-semantic or gameplay qualification.

## Contract

The indexed-reviewed candidate uses version 4 and a new pipeline fingerprint binding the existing per-plan source exclusivity instruction. It applies to initial planning and targeted plan repair while retaining all source actions, references, batch sizes and generation settings. The validator's acceptance set is unchanged; the model chooses the assignment. No factor or intended consequence is automatically deleted.

The shared foundation preserves deterministic causal failure observations and rejects dangling JSON fields without substituting an interior field value for the full candidate. These repairs retain the source state, rejected output and existing repair limits. They introduce no inference call and do not certify better live recovery. Historical failures and paid results remain immutable.

Admission checks the closed source-role pair and its source-bound review alongside the existing complete mechanical and canonical transition proofs. The source-role treatment passed one recorded repair case; timing, resource commitments and later behavior require source review on the new world. The candidate stays isolated from default interactive instances until full gameplay acceptance.

## Plan

After relevant tests, check:fast and local commit, freeze current request hashes, tokenizer admission, model metadata, complete world and a fresh trial ID. Preserve the forty-eight Agents, 232 entities and seed. Follow the [full-world run contract](0060-integrated-transition-gameplay-diagnostic.md), including per-step watchdog, CNY150 and 200-HTTP run caps, source review after each commit and fresh confirmation. Any prospective phase reallocation must fit the existing CNY1000 total, all incurred cost and unknown reservations, and a complete confirmation run. Do not reduce required samples to fit a budget or resume a failed trial.

## Verification

Exercise the registered pipeline to show the instruction appears exactly once on planning and repair requests and does not alter transition requests. Verify current pipeline identity and actual step/replay through the existing playtest tests. Require the frozen diagnostic hashes and reject changed or missing evidence. Preserve the real materializer, causal verifier and observer gates. Report combined-run outcomes without attributing any measured change to one component.

## Evidence

[Source-role transport tests](../../src/engine/mechanics/__tests__/resolution-source-role-contract.test.ts) own the instruction boundary. [The integrated launcher](../../scripts/operations/step-indexed-checkpoint-playtest.ts) binds admission evidence. [Causal feedback](../postmortems/0083-discarded-causal-failure-observations.md) and [JSON parsing](../postmortems/0084-dangling-json-field-salvage.md) have separate permanent regression guardrails.
