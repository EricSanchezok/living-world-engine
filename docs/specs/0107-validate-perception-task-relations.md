# Validate perception task relations and complete repair diagnostics

Artifact-Version: 1
Status: Approved

## Intent

Enforce the existing focused perception assignment and Rating ownership contracts at the real execution boundary under the continuing gameplay optimization authorization. Preserve model choice while rejecting checks for unrelated work before any random draw.

## Contract

When focused observer/source-action pairs are supplied, each proposed check must name one of those observers and cite at least one source action assigned to that observer. Additional causal evidence remains legal. An explicit empty assignment permits no check; an absent assignment retains the general complete-action task scope. A matching pair is only task membership, never evidence of sensory access, a justified check, or preserved natural-language meaning.

The existing owned observer Rating, owned opposing Rating, identical opposed source and single mechanical use requirements produce field-local diagnostics with source-bound legal handles. Independent draft errors across the complete batch are collected for one repair. Validation preserves the rejected output, full source context, original order, repair limits and model settings; it never chooses another observer, source action, aptitude, target or stakes. Optional null aptitude remains an explicit model decision, not an automatic correction.

All drafts pass relation validation and existing materialization before the batch consumes randomness. Remaining materialization errors are collected across independent requests. Accepted checks retain the existing runtime identity, numeric rules, RNG order and atomicity. The default pins onset-perception@4; optional rating-choice representation remains experimental and is not enabled by this repair.

## Plan

Validate focused output task membership and report complete ownership errors through the existing semantic repair interface. Preserve actual failed outputs as local reproduction evidence. Replay the frozen rule-control outputs and recorded complete-source mismatches through the actual revised entry without new model calls. After those guards pass, use a fresh canonical-baseline WorldHost trajectory to measure repair recovery and the real critical path, with source review after every commit. An optional representation does not inherit qualification from this foundation, and no replay is reported as fresh model success. Player-triggered verification remains mandatory for the gameplay goal.

## Verification

Exercise the real entry with wrong observers, mismatched actions, empty/unfocused assignments, coalesced assigned actions, additional causal evidence, independent owned/opposed/source errors and a valid repair. Verify exact paths and allowed handles in the subsequent physical request, all errors in one repair, unchanged input and identical clean/repaired checks and RNG. Replay the recorded mismatches without model HTTP and retain historical results unchanged. Run focused tests and check:fast before committing; correctness guards alone do not establish 60-second feedback.

## Evidence

[Perception entry tests](../../src/engine/mechanics/__tests__/perception-references.test.ts) own the real boundary. [Assignment](0102-preserve-perception-work-assignment.md) and [numeric ownership](0103-derive-perception-check-numbers.md) define the underlying contracts. [The incident report](../postmortems/0109-perception-output-escaped-task-assignment.md) records the missing guard.
