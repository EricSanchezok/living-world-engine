# Perception Visibility Branch Screen

Artifact-Version: 1
Status: Approved

## Intent

Test the observed confusion between an onset that is perceptible without a check and an onset supplying no stimulus. In the complete-source relation screen, explanations sometimes established direct participation or hearing while selecting no_stimulus. Neither low repair counts nor mechanically valid reports established correct perception.

## Contract

Append an explicit decision table to the existing perception postlude. Separate established access without required randomness, established absence of access, and supported uncertainty or an authored check requirement. A batch may contain different terminal report statuses. Preserve the complete canonical context, all 49 Agents and 35 assigned relationships, original schema, checks, prompts, examples and model configuration. This input-only experiment does not infer any relationship's verdict, select evidence, alter responses or change default execution.

## Plan

Use the complete initial perception source from the failed player run. Capture and freeze B/C/C/B requests before four new HTTP calls with no repair or continuation. Reconstruct B exactly. Pin the parent commit, complete source patch and producer hashes throughout dispatch. Report canonical parsing, existing-reference validation and the independently compiled relation screen separately from manual source semantics, tokens and latency.

## Verification

Verify exact unchanged context and schema, with only the declared postlude and its prompt identity changing. Reject producer drift. Review all assigned pairs, including direct conversation, remote intended communication, uncertain access and actor/observer distinctions. A mechanically accepted but semantically false no_stimulus report fails this screen. This small diagnostic cannot establish stable reliability or the full-player target in [Spec 0122](0122-player-action-efficiency.md). The [relation-domain screen](0146-perception-check-relation-domains.md) remains a separate negative candidate.

## Evidence

The existing complete-source probe records frozen requests, first responses, canonical rejection issues and the independent relation checks. Both perception screens preserve negative source semantics instead of qualifying terminal output by form alone.
