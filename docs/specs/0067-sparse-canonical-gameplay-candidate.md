# Sparse Canonical Gameplay Candidate

Artifact-Version: 1
Status: Approved

## Intent

Continue the [delegated non-thinking gameplay experiment](0029-nonthinking-gameplay-efficiency-experiment.md) with the declared representation from the [sparse-array diagnostic](0066-sparse-transition-array-diagnostic.md). The pair supplied one formal-admission improvement; completion semantics remain uncertain and require actual state inspection.

## Contract

Version 5 of the indexed-reviewed candidate applies the sparse codec after canonical evidence construction, on initial and repair requests. Only the four empty root effect containers may be omitted under the declared wire contract. Nonempty effects, nested requirements, original actions, complete source context and repair bindings remain intact. Physical indexed batches retain their existing representation. The canonical schema, materializer, causal verifier, observations and atomic kernel remain unchanged.

The pipeline fingerprint binds the sparse instruction and codec identity. Closed paired evidence and its source review must match before a fresh complete-world run. This admission is deliberately limited: it does not certify arbitrary action meaning, completed cognition or gameplay. The historical baseline remains failed; neither prior outputs nor saves are retroactively admitted under the new contract.

## Plan

Run relevant tests and check:fast, commit locally, and freeze the current complete mechanical and canonical source/body proofs before a new trial. Retain the [integrated contract](0060-integrated-transition-gameplay-diagnostic.md): forty-eight Agents, 232 entities, unchanged seed, full actions and batches, DeepSeek Flash with thinking disabled, existing repair limits, CNY150 / 200 HTTP per run, per-step watchdog and at least three credible commits plus fresh confirmation. Preserve all incurred costs and unknown reservations in the existing CNY1000 ledger. Check any prospective phase transfer against both a complete exploration and confirmation run.

After each committed step, inspect all original actions and their actual effects. For cognitive recording, distinguish an actor's claimed observation from canonical facts and verify downstream observation and cognition; an outcome summary or identity assertion alone is insufficient. False completion, lost conditions, knowledge leakage or required-effect omission veto acceptance. No default interactive instance is switched before full acceptance.

## Verification

Exercise the registered pipeline for both initial and repair sparse responses, canonical round trips and retained nonempty effects. Use the real source-bound movement oracle to veto a missing-effect mutant even if scripted surrounding roles let it commit. Preserve complete mechanical batch preflight and source/repair restoration, registry identity, actual transport controls and replay tests. Report formal improvement separately from semantic uncertainty, cache effects, token count, latency and total costs.

## Evidence

[Sparse tests](../../src/engine/mechanics/__tests__/canonical-sparse-arrays.test.ts) own gateway and behavior checks. [The paired runner](../../scripts/experiments/step-sparse-array-probe.ts) owns source reconstruction and dispatch ceilings. [The integrated launcher](../../scripts/operations/step-indexed-checkpoint-playtest.ts) owns closed-evidence admission and fresh full-world identities.
