# Transition Evidence Worklist

Artifact-Version: 1
Status: Approved

## Intent

Within [0029](0029-nonthinking-gameplay-efficiency-experiment.md), make complete batched transition responsibility and its adjudication evidence directly readable. The [indexed transition diagnostic](0049-indexed-transition-diagnostic.md) remains failed; this is a separately frozen candidate with explicit source-evidence layout and bounded runtime recovery.

## Contract

Regroup wire output before canonical validation so a rejected slot cannot erase already valid neighbors or force their regeneration. Retain the complete original shared context, action cardinality, reference domains, generation parameters and canonical output language. Enrich the existing source action index with all matching committed plans, receipts and temporal activities, the original boundary, and the exact input facts explicitly cited by those records. Join only on existing action and fact references; do not rank, truncate, invent evidence or choose effects. Other original evidence remains available and authoritative. Each row is a hash-verifiable view of its original slot, not a complete semantic proof or an expanded permission domain.

The physical envelope's empty assignment and empty reference catalog are batching metadata, not the logical slot's permissions. Relocate those exact placeholders into explicitly labeled transport audit metadata while preserving their values for a deterministic inverse. Point the physical task at the complete work index and explain that existing references remain governed by the original slot catalog. Preserve each logical slot's real assignment, reference catalog and repair context without changes. Retain the [provisional receipt contract](../postmortems/0076-provisional-receipts-and-action-completion.md).

## Plan

Use the real TruthBatchCoordinator to reconstruct the original twelve-slot, forty-three-action source from trajectory-e2-09. Freeze its initial physical request and verify expanded logical source equality. The candidate reuses indexed output and the shared-context transport. Run one fresh full root with at most one existing structural repair, two HTTP total, no transport retry, extra critic, warmup or redraw. Charge the complete ceiling to the existing STEP-E2 budget and retain raw evidence, partial reports and unknown reservations. This selected-failure feasibility check prioritizes correct final coverage within bounded recovery; report first-call success separately and make no comparative efficiency claim.

A complete formally valid root only permits independent inspection of all source actions, actual proposed effects, timing, quantities, audiences and knowledge boundaries. Missing or uncertain evidence cannot justify promotion. A new full-world trial requires separate frozen code, all runtime semantic gates and source review after every commit. Earlier no-repair failures remain failures and are not retrospectively admitted by this criterion.

## Verification

Check exact restoration of the full physical source, complete source joins, source hash binding, source immutability, conflicting metadata, scoped facts and missing source records. Exercise the actual batch coordinator and gateway with a rejected empty first response followed by an explicit complete repair, preserving all original logical slots and identity mappings. Verify maximum HTTP, original error retention and partial reports. Run focused tests and check:fast before committing an independent unit.

## Evidence

The [bounded source launcher](../../scripts/experiments/step-transition-worklist-probe.ts) reconstructs and hashes the complete original request before paid dispatch, rejects source drift or splitting, and records per-slot results and partial accounting through the shared transport. Its admission decision explicitly separates complete formal coverage from subsequent source-semantic review.

The STEP-E2 local evidence directory holds immutable original Ledger payloads, prospective source/request fingerprints, raw responses, per-slot validation, independent semantic records and the existing append-only cost ledger. Repository regressions establish deterministic representation and recovery properties; they do not establish model behavior or gameplay.
