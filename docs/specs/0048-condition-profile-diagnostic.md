# Condition Profile Diagnostic

Artifact-Version: 1
Status: Approved

## Intent

Within [0029](0029-nonthinking-gameplay-efficiency-experiment.md), test the existing indexed planning contract with precise condition-profile feedback on the latest failed complete physical root before another expensive whole-world run. [Postmortem 0074](../postmortems/0074-condition-effect-profile-feedback.md) records the observed mechanism and deterministic repair regression.

## Contract

The frozen source is trajectory-e2-08's original four-slot, four-action planning root. Retain its complete 48 available actions, 232 entities, canonical truth, action groundings, source inventory and temporal boundary. This is a selected failure diagnostic, not a smaller gameplay configuration. Content hashes bind the source input, final preparation and original physical request. Reconstruct activity insertion order from the recorded preparation, then verify canonical projection, original action identity and full source information against the captured contexts.

Keep source-indexed planning, worklist, dependent fields, full-context transport and DeepSeek v4 Flash with thinking disabled. The shared materializer reports condition/duration family and pairing errors at their draft fields without choosing replacements or dropping effects. A new neutral cache namespace separates the fresh diagnostic; actual cache hits remain measured. No paired or first-pass improvement claim follows from a historical comparison.

## Plan

Freeze the prospective request through the actual admission entry path. Run one fresh initial request and at most one repair, two paid HTTP total, with no transport retry, warmup, response replay, semantic verifier, RNG or world commit. Charge all calls to the existing STEP-E2 probes phase. Preserve first-response and final coverage separately and stop on budget or billing uncertainty. Do not restart or overwrite the failed whole-world trial.

All four plans must pass formal admission with settled usage before source-bound semantic inspection. Examine the original goals, available time, claimed receipt of communications, prerequisites, affected subjects, effect timing and no-effect substitutions. Mechanical success is neither semantic certification nor gameplay acceptance. A failed or uncertain diagnostic cannot justify candidate promotion; a new complete-world run still requires its mandatory live gates and per-commit inspection.

## Verification

The launcher verifies exact frozen source hashes, model and registry bindings, full world cardinality, four original slots, reconstructed source equivalence, unchanged output schema and the first actual request hash. The decision regression rejects incomplete action or slot coverage, unknown usage, changed cell identity, duplicate rows and excess HTTP. The shared admission runner retains raw evidence, partial reports, request accounting and interruption handling. Run relevant tests and check:fast before commit and record the freeze and result in the existing work record.

## Evidence

The [diagnostic launcher](../../scripts/experiments/step-condition-profile-probe.ts) owns the frozen source identities and prospective request checks. The independent STEP-E2 evidence directory retains original Ledger artifacts, generated requests, raw responses, per-slot admission, semantic inspection and the append-only cost journal. Historical failures remain unchanged.
