# Transition Source Segment Selection

Artifact-Version: 1
Status: Approved

## Intent

Within [0029](0029-nonthinking-gameplay-efficiency-experiment.md), remove copying errors from the [0055](0055-transition-current-interval.md) evidence interface while retaining its current-interval decision structure. Preserve the closed diagnostic's quote mismatch and pending-intent failure.

## Contract

The unpromoted worklist enumerates punctuation-delimited text segments for each original action, retaining every character, delimiter and whitespace in order. Concatenation equals the complete original rawText exactly. This table defines citation boundaries only; it does not interpret conditions or classify actions. Each model-authored gate selects one or more increasing contiguous unique indices from its own action's table instead of copying sourceText. Reject missing, duplicate, skipped, reversed or foreign indices and modified tables. All original source text, evidence pointers, values and canonical output fields retain their contracts. Do not correct or admit historical misquotes automatically.

Pending work must retain the original action's negations, time bounds, quantities and audience. It cannot transform a prohibition into an optional action or add tasks from broader goals and background. This is an explicit model instruction and independent review obligation, not a claimed deterministic semantic validator. Keep thinking disabled, the same model and generation settings, full action and slot cardinality, no extra online planner or critic, and the existing budget ledger.

## Plan

Freeze one new complete twelve-slot/forty-three-action source diagnostic after focused and full checks. Preserve exact-value evidence matching, currentWork-to-summary restoration, candidate prefix representation and the at-most-two-HTTP ceiling. Measure input/output tokens and latency, then separately evaluate formal fidelity and original-action semantics. Reject any unmet gate before gameplay promotion; all prior outcomes remain intact.

## Verification

Exercise multilingual punctuation, quotes, spaces, line endings, decimal quantities and combining characters; verify exact text concatenation and stable ordered indices. Reject unknown, missing, duplicate, skipped and reversed selectors or modified source segments. Use the full worklist/gateway regressions for retained neighbors. Verify complete request tokens offline and run check:fast before committing.

## Evidence

[Interval assessment tests](../../src/engine/mechanics/__tests__/transition-interval-assessment.test.ts) cover segment fidelity and selector rejection. [Worklist tests](../../src/engine/mechanics/__tests__/transition-evidence-worklist.test.ts) cover canonical restoration and actual gateway handling. The local experiment record retains source snapshots, raw model decisions, independent reviews and accounting; selection fidelity alone does not establish semantics or gameplay.
