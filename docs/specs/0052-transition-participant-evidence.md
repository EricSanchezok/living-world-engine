# Transition Participant State Evidence

Artifact-Version: 1
Status: Approved

## Intent

Within [0029](0029-nonthinking-gameplay-efficiency-experiment.md), address source-state prerequisites lost by the formally complete [0051](0051-transition-required-assertion.md) diagnostic. Its proposal asserted an interview across distinct locations and post-funeral deliberation while the funeral remained active. Keep that semantic rejection intact.

## Contract

Extend the existing [0050](0050-transition-evidence-worklist.md) reversible evidence view. For each original action, select the canonical actor and target entity references already present in its committed plans. Copy their exact visible entity records, every original fact whose subjectRef is one of those entities, and each entity's visible placement ancestor chain. Union these facts with all previously cited facts; do not rank, truncate, summarize, infer prerequisites or choose operations. The original complete state and slot reference catalogs remain authoritative and unchanged. The view grants no new reference permission or Agent knowledge. Missing participant entities, malformed placement edges and cycles are configuration failures, not reasons to silently drop evidence.

Explain the distinction between permission to attempt an action and evidence that its physical, timing or audience prerequisites are satisfied. An active Activity or an automatic receipt does not imply that every subtask has started. Preserve ordered and conditional source text. A continuing outcome may describe preparation or waiting; claims of performed interaction or completed substeps require supporting state or proposed effects. Do not require an artificial write every tick or invent contact, communication, knowledge or completion to fill the output.

## Plan

Replace the unpromoted worklist view with a versioned participant-state join. Freeze a new complete twelve-slot/forty-three-action diagnostic on the same source with all original generation settings and disabled-thinking Flash. Keep the existing at-most-one structural repair/two-HTTP ceiling, no transport retry or redraw, and the existing STEP-E2 budget ledger. Verify complete request size and source integrity offline before dispatch. A formal pass permits independent semantic review only; any unresolved source violation prevents gameplay promotion. Earlier rejected results remain rejected. A fresh full-world test requires separate frozen code and per-commit source review.

## Verification

Test exact subject-fact inclusion even when a fact was not cited by a permission plan, participant placement ancestry, absence of unrelated facts from the additional view while retained in full context, exact inverse restoration, immutable source, conflicting derived evidence, missing entities and cyclic placement rejection. Through the actual coordinator and gateway verify complete source responsibility, disabled thinking and valid-neighbor preservation. Run focused tests and check:fast before committing and freezing any paid request.

## Evidence

The local worklist-02 raw proposal, original state and source-review record preserve the rejected meeting and funeral cases. The [worklist regressions](../../src/engine/mechanics/__tests__/transition-evidence-worklist.test.ts) verify mechanical joins without model-authored gold labels. Neither this layout nor formal schema admission establishes open-semantic correctness or gameplay acceptance.
