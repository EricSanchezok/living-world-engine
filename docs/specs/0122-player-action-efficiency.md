# Player action efficiency

Artifact-Version: 1
Status: Approved

## Intent

Make complete player actions in the bundled world finish within 60 seconds with near-zero semantic repair, preserving the original action freedom, world activity and grounded consequences. The user authorizes algorithm and architecture experiments, local implementation and commits, research records and real-player testing in the current worktree.

## Contract

Use all 48 autonomous Agents plus one external player and the configured DeepSeek Flash model with thinking disabled. Measure from player submission through completed Activity, atomic world persistence and player-visible feedback. First feedback, a checkpoint, a mechanically valid candidate and an offline replay are separate observations, never substitutes for complete action latency. Keep the complete source actions and world semantics; do not suppress NPC activity, convert uncertain checks to automatic success, fabricate effects or remove semantic validation to meet the target.

Experiments retain immutable source identities, complete requests and responses, model settings, code revision, initial rejection, repair recovery, terminal failure, input/output/cache usage and timing. Each prospective comparison fixes its arms and call ceiling before network access. The new approximate CNY 1,000 authorization is tracked separately from historical research expenditure. Existing evidence may establish a negative baseline without repeating initialization; model-visible candidates need fresh source-bound comparisons before adoption.

The initial candidate clarifies that check effects are conditional stakes committed before randomness, not assertions that success has already happened. It changes instruction wording and schema descriptions only, retaining every field, value domain, action, context and validator. It cannot infer effects or resolution modes in code. An unchanged baseline and this candidate run on the same complete source request; full source-semantic review follows mechanical admission.

The profile-domain candidate specializes effect profile fields from the complete source catalogs and their authored mechanic collections. Its physical union includes every eligible duration, condition and impact profile, retains null for open conditions, and preserves the original field predicates and per-slot validators. It adds no output rewriting. The control carries the conditional-stakes wording; the treatment adds finite profile choices and explains the existing risk-owned threatened magnitude. Neither arm may fabricate a profile or accept an unknown namespace.

The action-frame candidate joins each assigned action to its exact actor Entity and existing Activity at the current temporal boundary, placing this complete source evidence beside the action assignment. This additive projection preserves the original context and output schema, rejects ambiguous ownership and changed source bindings, and supplies no interpreted progress or outcome. Its first comparison uses the profile-domain candidate as control and adds only the bound frames and their interpretation instructions. Source-semantic review must distinguish another Agent's similar intent and a future Activity objective from the current actor's work in the current interval.

## Plan

Truth actor projection preserves the exact relation between issued local references and their canonical entity bindings, including unresolved and multiple bindings, alongside the complete existing inventories. A deterministic regression must distinguish two worlds with identical labels and inventory order but swapped pairings. Private AgentMind and ordinary client projections retain their existing cognitive boundary. Version the model context and execution producer for this source-contract correction; empirical model improvement remains a separate acceptance requirement.

Capture the registered production composition at the failed player's planning boundary. Verify historical output through that same entry path. Compare frozen requests through the real model gateway and preserve rejected responses. Adopt only demonstrated improvements, then run independent continuous player scenarios through WorldHost and the browser. Investigate remaining serial generation, redundant work and task completion from measured critical paths.

## Verification

The player measurement binds terminal feedback to the submitted participant, source revision, complete action text and registered run Activity. Only a canonical `completed` Activity populates `completedElapsedMs`; a stopped run or an `awaiting-decision` boundary with unfinished work records its end time separately. Committed prose and a conversation turn marked committed do not establish action completion. Real WorldHost regressions cover completed work, model failure, a continuing Activity stopped by the lease budget and an interrupted Activity awaiting another decision. The measurement implementation is included in the prospective source hash binding. This mechanical completion check does not replace independent review of goal achievement and world effects.

The cognition-integrated player diagnostic pins the existing single-text producer and restricted unmatched-closer parser in a new diagnostic root identity. Bootstrap and mind requests use the single-text action contract; bootstrap, mind and reaction requests select the existing local parser policy through their actual provider path. Canonical schemas, complete slot coverage, private reference permissions, inference settings, repair limits and all other role requests remain unchanged. The parser's [existing syntax contract](0031-unmatched-closer-recovery-experiment.md) retains ownership of allowed edits and rejects unsupported transports. A provider-bound regression must run registered bootstrap against malformed complete responses, verify exact text and target materialization, and still reject an invalid private reference after syntax recovery. Existing persistence and external-reaction checks must retain the new producer identity. A fresh full 48-plus-player WorldHost diagnostic uses no imported model outputs, preserves all source actions and records initial rejection, local syntax normalization and model repairs separately. The single-text embedding's duplicate canonical text remains a measured cost, not an assumed saving. This combined diagnostic does not promote either candidate or replace the complete player acceptance criteria.

The single-text AgentMind screen changes the model-authored action contract to one self-contained `rawText` plus the original `targetHandles`. The text carries the chosen attempt, desired result or continuing condition, method when specified, uncertainty and deliberate compound or conditional work. Private strategic goals, belief and character patches remain unchanged. Its experimental decoder copies the exact text to canonical `goal` and sets `means` to null, matching the existing external-input embedding; it never infers a separate goal, edits prose, accepts discarded fields, or transforms persisted actions. This is a new generation contract, not a lossless representation of historical three-field actions. Baseline offline replay retains exact original HTTP bytes and historical materialization. Candidate offline fixtures explicitly concatenate all three historical texts, retain patches and target handles, and verify the exact new text embedding through AgentMind; they are synthetic transport fixtures, never empirical semantic success or equivalent historical normalization. Bind fixture content separately. Freeze the original six batches and 48 NPCs for twelve alternating primary-only B/C calls with complete audit, usage and failure retention. All generated actions need source review before downstream qualification; bootstrap timing is not player latency. [Decision 0177](../decisions/0177-generate-one-authoritative-action-text.md) owns the experimental contract and alternatives.

The Agent action-scope screen preserves the six original bootstrap batches and all 48 original NPCs through the real AgentMind entry point. B retains the original prompt; C adds only source-situation and action-triplet instructions plus descriptions for raw text, goal and means. No fields, value domains, contexts, target permissions, validators or batch sizes change, and compound, conditional and ongoing intentions remain available. Reconstruct the complete pre-bootstrap world from its immutable source assets and verify its hash. Offline B HTTP bytes, both arms' final canonical normalization and all materialized historical commits must match before freezing twelve new primary HTTP calls, one per batch and arm in alternating order. Block repairs and retries before network, preserve failed outputs and their actual recovery demand, and stop later dispatch on missing usage or final audit. Review every generated candidate against its own identity, present situation and evidence, checking self-contained goal prose and agreement among its three action texts. Different autonomous choices are legal; an arbitrary textual match with the baseline is not the semantic oracle. Bootstrap work and imported history never count as complete player action latency. No critic inference, inferred field correction or resampling is part of this screen. [Decision 0176](../decisions/0176-keep-agent-action-texts-in-one-scope.md) owns the alternatives.

The onset-domain screen uses the complete original 49-action, five-batch source and the same ten-call, primary-only alternating B/C protocol as the field-domain screen. C checks the actual grounding resolver's complete Fact/Entity domain with the original causal evaluator, binds its source state and request epoch, and omits only the uninhabited record-absence branches from onset wire guidance. All other values, canonical schemas, output processing and validators remain unchanged. Offline replay must preserve every historical request byte in B and every historical acceptance, rejection and materialization in both arms, including a model returning an omitted branch. Present records with none or false values remain distinct from absent records; later-state Truth assertions retain their original vocabulary. No replacement prerequisite or temporal choice may be inferred. Full semantic review considers the complete source action triplet and records upstream contradictions rather than assigning all downstream disagreement to the compiler. [Decision 0175](../decisions/0175-specialize-uninhabited-onset-branches.md) owns the alternatives.

The typed-alias screen uses the complete original 49-action, five-batch source and the same primary-only B/C comparison limits as the field-domain screen. B retains the historical generic AT aliases; C prefixes exact aliases with the actual catalog kind without field-domain additions. Re-encode complete historical C responses only for offline replay, prove exact inverse values and unchanged canonical normalization, slot validation and materialization, and distinguish the intentionally different raw wire hashes. The real parser and the captured request's syntax policy recover historical values before offline re-encoding; retain the original text hash and recovery classification. The generated replay is a representation check, not evidence that C avoids a historical syntax error; B still exercises the exact original response. Preserve prose and opaque literals, reject changed source bindings and invalid prefixes, and freeze both requests before ten new primary HTTP calls. Actual first-pass admission and full source-semantic review remain mandatory before a live player continuation. [Decision 0174](../decisions/0174-type-compilation-alias-names.md) owns the alternatives.

The strict-tool readiness screen measures the provider's documented Beta endpoint before building a game adapter. It fixes eight tiny controls and the current Flash model with thinking disabled: two paired adversarial controls differ only in `function.strict`, followed by individual tests of union/intersection keywords, exact array cardinality and open objects present in the game's schema. Preserve complete credential-free request bodies, raw responses, status, usage and code/catalog/metadata bindings. No tools are executed. HTTP success, syntactically valid arguments, a compliant non-strict control or a server schema rejection does not establish enforcement. Missing successful-response usage, unexpected HTTP failures and transport errors stop subsequent dispatch; HTTP 400 is an explicit rejected request with unreported usage, not a successful zero-cost inference. Do not retry, switch models, weaken the game schema or infer full-scene reliability from these controls. Capability support only authorizes designing a separately validated adapter with complete canonical value coverage and the original compiler checks.

The compilation field-domain screen retains all five first-call batches, totaling 49 actions, from one immutable player preparation. B uses its recorded AT compilation contract; C adds shared schema definitions for the complete current visible union of state-dependency, audience and resource-pool choices, with original predicates and per-slot validators intact. Offline replay must reproduce every B HTTP body byte-for-byte and retain identical normalized historical outputs, acceptance and materialized compilations under both arms. The independently recorded initial state may restore object-key order lost during Ledger export only after exact canonical value equality with every captured source; bind that original order separately because profile evidence embeds JSON strings and diagnostic choices follow catalog order. Freeze both requests and code before HTTP. The new screen permits exactly one primary per original batch and arm, at most ten new calls with alternating arm order; block repair and transport retries before dispatch. Preserve original failed responses, blocked recovery, input/output/cache usage and phase wall time. Missing terminal audit or usage stops subsequent dispatch. All 49 candidate actions must first-pass and pass full source-semantic review before any full-player continuation; one paired cohort cannot establish reliability or a causal latency improvement. [Decision 0173](../decisions/0173-share-compilation-field-use-domains.md) owns the representation alternatives.

A combined full-player diagnostic uses a new isolated catalog and data root, the complete bundled world constructed through the [finite-work and short-checkpoint recipe](0138-preserve-finite-work-experiment-world.md), and the current standard composition as an explicitly registered diagnostic root. The root pins conditional stakes, complete effect profile domains, source action frames, finite factor products and the exact observation dictionary. It does not promote these candidates or attribute a combined outcome to one adapter. The actual executing object owns the same registered producer when preparing, persisting and resuming an external reaction; a wrapper manifest alone is insufficient. Preserve every original Agent, action, catalog, output validator and full-world semantic review; no historical preparation or model output is imported into this live trajectory. Create the external harbor participant through WorldHost, verify 48 original Agents plus that participant, and submit the lodging inquiry through the actual player action path. Measure each persisted feedback and whole action completion separately. The prospective ceiling is 120 new inference HTTP requests, ten minutes before blocking later dispatch, and six commits per action lease; drain already-running calls, retain all repairs and fallbacks, and stop on a terminal failure. Source review of actual goal achievement is mandatory before another player action. A first-feedback checkpoint, a lease pause or nominal completed run without a grounded answer or obstacle does not satisfy the goal. Existing historical samples remain immutable.

The observation action dictionary candidate interns exact complete records and ordered action sequences at the established shared and slot-delta action-set paths. Every source record, occurrence, ordering and observer-specific reference remains exactly reconstructable; original output schemas, decoder closures, materialization and information-boundary checks remain unchanged. The source-bound probe imports a recorded accepted transition and reconstructs all 49 observations through the actual renderer and physical batching. Offline admission requires exact ordered matches for all five historical requests and identical materialized historical packets through both representations. Its fixed B/C screen permits at most ten new primary observation HTTP calls, five per arm, stopping before repairs; input token reduction, wall time, output volume, normalization, source semantics and terminal failures are reported separately. One pair only screens feasibility. Imported upstream work and a mechanically valid observation cohort do not qualify as player action completion or persisted gameplay. [Decision 0171](../decisions/0171-intern-exact-observation-action-sequences.md) records the alternatives.

A sampled-planning continuation binds an accepted normalized result and its original audit to the exact ordered request, source preparation, model catalog and current experimental producer. It imports this evidence explicitly, counts its transport and preparation work as historical, and permits at most 16 new primary downstream HTTP calls through the real SimulationEngine. Repairs and replacement planning stop before network access. Preserve every downstream request, response and issue, await already-running siblings, and run canonical validation before reporting in-memory acceptance. Neither imported work nor an in-memory commit qualifies as complete player-action latency or persisted gameplay.

A separately frozen repair diagnostic continues the exact recorded first review, including its reject verdict and findings, without redrawing or overriding that result. It permits at most 24 new HTTP calls and four model repair requests through the unchanged production recovery and canonical validation paths. Original planning and review costs remain historical; every new rejection, recovery and semantic change remains part of the diagnostic. Its purpose is to expose the downstream critical path and whether repairs preserve supported intent, never to demonstrate first-pass success or promote an experimental planner.

The complete-factor product candidate compiles every finite direction and variable authored step count into an explicit factorType choice, preserving all legal canonical factors through a bijection. Source authority, channel, explanation, source ownership and all other plan fields remain unchanged. Never infer a missing choice or remove its numeric influence. Its two-candidate feasibility screen requires the earlier B request to match the reconstructed baseline byte-for-byte in order-sensitive evidence before HTTP. Both candidates use the unchanged full context and current counterfactual producer. Reuse of a historical control cannot establish a contemporary latency effect or stable success rate. [Decision 0170](../decisions/0170-select-complete-finite-factor-choices.md) records the alternatives.

The decision-order screen places the action, resolution mode, actor rating and difficulty before dependent factors and effects in the wire schema and generation instructions. It preserves every field, predicate, original input and decoder. Its two-call B/C feasibility screen uses the current producer with an explicitly recorded counterfactual import of the source preparation; only its producer binding changes, never a persisted save. B retains conditional stakes, profile domains and action frames; C adds governing-choice ordering. Preserve order-sensitive request hashes. A single pair cannot establish reliability or an isolated latency effect; review every mechanically admitted result against source semantics before any continuation or promotion.

Check exact source and candidate preservation, unchanged schema predicates and failed-output rejection. Run focused regressions and check:fast before each implementation commit. Full acceptance requires fresh complete player trajectories, per-action timing under 60 seconds, near-zero repair, and source-bound review of intended task completion, causal changes, time, information access and the visible response. Record every counterexample; finite samples do not prove universal semantic correctness.

## Evidence

The [standard composition](../../src/engine/algorithms/standard-composition.ts) owns the production baseline. The [debugging reference](../debugging.md) owns durable execution evidence and the [testing reference](../testing.md) owns verification boundaries. Online acceptance is pending.
