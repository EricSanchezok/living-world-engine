# Reviewed Truth Transport Playtest

Artifact-Version: 1
Status: Approved

## Intent

Under [0029](0029-nonthinking-gameplay-efficiency-experiment.md), connect the source-tested plan candidate to a fresh full-world diagnostic. Admission and existing same-family semantic review passed for both complete original roots; neither is evidence of accepted gameplay steps.

## Contract

An explicitly selected candidate Composition pins shared-json-v3, shared-state-first-v1, unmatched-closers-v1, the existing physical cardinality/no-example contract and repair-tail layout. It reuses the same policy boundary as the source admission runner. Dependent plan fields, source-owned compilation, grounding, full root cardinality, model settings, thinking-disabled execution, recovery bounds and transaction checks remain intact. No runtime or game API default is promoted. Existing candidate identities retain their original contracts.

The selected Truth coordinator uses the complete shared codec across its planning, verification and transition stages. The member-order policy applies only to shared envelopes; ordinary singleton rendering is unchanged. Restricted syntax recovery preserves its existing rejection boundaries. This broader composition requires actual world execution, source-action behavior review and replay; source planning success does not certify other stages. Observation and cognition Compositions remain unchanged.

## Plan

Bind both complete captured source reviews to their exact protocol/result hashes and verify all original slots and plans accepted. Preserve historical runs. A new trajectory uses an independent data directory and frozen Composition, world, registry, code and budget. Every model request remains in the existing E2 ledger. Incomplete steps, unknown billing or failed behavior checks stop that run and produce a partial report. Do not restart old trajectories or silently weaken validation.

## Verification

Verify registry resolution and identity, transport selection at the actual runtime provider boundary, singleton behavior, complete shared-context expansion, real state commit and replay. Preserve previous Composition hashes when the option is absent. Run relevant tests and check:fast before local commit. Full-world acceptance still requires three credible steps, fresh confirmation and measured avoidable-cost reduction under the encompassing protocol.

## Evidence

[Decision 0124](../decisions/0124-compose-reviewed-truth-transport-for-playtests.md) records the limited rollout scope. [0031](0031-unmatched-closer-recovery-experiment.md), [0032](0032-shared-state-prefix-layout-experiment.md) and [0033](0033-resolution-plan-evidence-contract.md) retain ownership of the individual behavior contracts. Experiment records own source, result, review and cost hashes.
