# Transition Catalog Order Prefix

Artifact-Version: 1
Status: Approved

## Intent

Within the delegated [0029 experiment](0029-nonthinking-gameplay-efficiency-experiment.md), remove observed duplicate reference-order text from the full transition request while preserving all action semantics and permissions. The frozen source contains twelve catalog orders sharing 2026 initial handles. This is a representation experiment, not a semantic repair or gameplay promotion.

## Contract

The unpromoted transition worklist stores the longest identical catalog-order prefix once and every original suffix under its original order identity. Concatenation restores the complete ordered list. Retain all candidate records, ranks, fields, source contexts and hashes; do not sort, select, summarize or alias references. A distinct codec identifier prevents accidental treatment as the original shared JSON. Restoration validates every original logical context hash and rejects corrupt or noncanonical representations. Explain full prefix-plus-suffix permission scope to the model. Canonical output, validation, model and disabled-thinking settings remain unchanged.

## Plan

Verify inverse identity and complete actual-message token counts offline. Freeze one new full twelve-slot, forty-three-action source diagnostic with the existing at-most-two-HTTP ceiling and STEP-E2 accounting. Compare input tokens with the closed source-matched worklist-04 diagnostic, keeping its semantic rejection intact. Formal success requires independent source-semantic review before any gameplay test. This trial changes only reference-order representation and its explanation.

## Verification

Exercise different-length orders, identical orders, empty orders, differing ranks, corrupt prefixes, unknown or duplicate handles, omitted orders and invalid split points. Verify the existing full coordinator/gateway path and exact original source restoration. Run focused tests and check:fast before commit and paid freeze.

## Evidence

[Prefix codec tests](../../src/engine/mechanics/__tests__/shared-catalog-prefix.test.ts) and [full worklist tests](../../src/engine/mechanics/__tests__/transition-evidence-worklist.test.ts) bind reconstruction and recovery. Local experiment evidence retains complete actual requests, tokenizer results, responses, semantic reviews and usage. Reduced input size does not establish semantic correctness or continuous gameplay.
